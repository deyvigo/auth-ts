info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{current_url}}/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "testuser2",
        "password": "password",
        "name": "Deyvi",
        "last_name": "Gomez"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
