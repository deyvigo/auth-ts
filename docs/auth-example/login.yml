info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{current_url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "testuser1",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
